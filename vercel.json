{
  "version": 2,
  "name": "stacking-cubes-math6",
  "buildCommand": "node -e \"const fs=require('fs'); fs.writeFileSync('env.js', 'window.__FIREBASE_CONFIG__ = ' + JSON.stringify({apiKey: process.env.FIREBASE_API_KEY||'', authDomain: process.env.FIREBASE_AUTH_DOMAIN||'', projectId: process.env.FIREBASE_PROJECT_ID||'', storageBucket: process.env.FIREBASE_STORAGE_BUCKET||'', messagingSenderId: process.env.FIREBASE_MESSAGING_SENDER_ID||'', appId: process.env.FIREBASE_APP_ID||''}) + ';');\"",
  "cleanUrls": true,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
